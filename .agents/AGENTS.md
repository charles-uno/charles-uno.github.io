# Tone

Keep it terse

Keep it honest

Keep it constructive

# Running Locally

To launch the site at `localhost:4000` run:
```bash
make
```

To launch it with drafts, run:
```bash
make drafts
```

After running locally, make sure to clean up after yourself! If you leave Docker containers running then nobody else will be able to use that port


